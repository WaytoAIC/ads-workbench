# 数据格式：把自己的报表放进工作台

一句话：**一份后台导出的搜索词报告，加三张你手填的小表，就能跑。** 其余五张表可选，没有就少几类动作，不会报错。

```bash
python3 _脚本/import_my_data.py 我的数据 ~/ads-lab-mine      # 整理成工作台能读的样子
python3 工作台/app.py --root ~/ads-lab-mine --port 8811     # 用自己的数据开一个工作台
```

模板在 `我的数据-示例/`，每个文件的表头照抄即可。真实数据只留在你自己电脑上。

## 必需的四样

| 文件 | 从哪来 | 列 |
|---|---|---|
| `搜索词报告.csv` | 广告后台 → 报告 → 创建报告：商品推广（SP）／搜索词／按天／最近 30 天 以上／下载 CSV。**英文或中文表头都认**，认不出会把它的表头打印给你 | 必有：日期、广告活动、客户搜索词、曝光量、点击量、花费、7 天总销售额、7 天总订单数。可有：投放、匹配类型、7 天总销量 |
| `周度经营.csv` | 你手填，每行一周，至少 2 行、建议 4 行。件数和销售额看业务报告；广告费看广告后台按类型汇总；毛利率自己算 | 周起始日、件数、销售额、SP广告费、SB广告费、SBV广告费、退款件、广告前毛利率（写 48% 或 0.48 都行） |
| `库存.csv` | 你手填，每行一个 SKU。可售看 FBA 库存页；销量看业务报告 | SKU、可售、上周销量（必填）；调拨中、途中、上上周销量、上周退款、上周广告费、上周销售额（可空） |
| `产品.json` | 你手填 | `品牌`（用来保护自家品牌词，永不否）、`客单价`、`父ASIN`（可空）、`显示产品` |

外加两份配置：

| 文件 | 说明 |
|---|---|
| `参数.json` | `目标ACOS`（0.30）、`补货到仓周数`、`产品阶段`（新品期／成长期／成熟期）、`精确活动`／`词组活动`／`广泛活动`（**必须是报表里真实存在的广告活动名**）、`显示店铺`、`显示产品`、`取舍人`。可选：`取舍项`（默认 A B C E）、`复盘日` |
| `分组规则.json` | 你这个品类的五类词表（正则）：`COMP` 竞品品牌、`OFF` 跑偏、`INTENT` 有购买意图的词根、`MOD` 属性词、`CORE` 核心词；`SKU正则` 用来识别同规格的另一款（没有就写一个匹配不到的）。**不要照抄示例的词，那是另一个品类的** |

## 可选的五张表（没有就跳过对应动作）

| 文件 | 列 | 没有它会怎样 |
|---|---|---|
| `活动.csv` | 广告活动、日预算、投放类型（自动／手动） | 到仓很快时出不了「下调预算」，只会提醒你补预算数 |
| `关键词竞价.csv` | 广告活动、关键词、匹配类型（broad／phrase／exact）、竞价、状态 | 出不了「降竞价」；三栏表里不判断竞价一刀切。来源：广告后台批量操作导出，或手抄主干活动的关键词 |
| `否定词.csv` | 广告活动、否定词、匹配类型 | 「否词没否干净」这条看不出来；换道否定可能与你已有否词重复 |
| `广告产品.csv` | 广告活动、SKU、ASIN、状态 | 出不了「暂停广告产品」和「补挂」；库存异常仍会报，但落不到动作 |
| `退货.csv` | 原因、件数（或一行一件） | 退款率仍会算，但不知道为什么退 |

## 窗口怎么定

导入脚本按搜索词报告的最后一天往前数：近 14 天＝「近」，再往前 14 天＝「前」；复盘日默认＝最后一天 + 8 天。搜索词报告至少要覆盖 3 周，建议 4 周以上。最近 3 天归因未满，订单会偏少，报告里会标出来。

## 常见报错

| 报错 | 怎么办 |
|---|---|
| 搜索词报告里认不出这些列 | 按它打印出来的表头，把列名改成中文（日期／广告活动／客户搜索词／曝光量／点击量／花费／7天总销售额／7天总订单数） |
| 参数.json 里 精确活动＝「…」不在报表的广告活动里 | 照它列出的活动名改，一个字都不能差 |
| 搜索词报告只覆盖 N 天 | 后台重新导 30 天以上的报告 |
| 搜索词报告与活动报告花费相差超过 1% | 只出现在你额外放了活动报告时：两份报告日期范围没对齐 |
