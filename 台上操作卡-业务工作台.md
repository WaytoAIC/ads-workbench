# 台上操作卡 · 本地业务工作台（广告周检，约 7 分钟）

定义一句话：**一条流程、明确的使用者、一处数据存储、一个操作入口，Agent 处理、人检查。**
这里的对应：流程＝广告周检五步｜使用者＝老板／广告运营／供应链（右上角切换）｜存储＝本机一个 SQLite 文件｜入口＝这个网页｜Agent＝「跑一圈」背后那条流水线。

## 启动
双击仓库里的 `启动工作台.command`，浏览器自动打开 http://localhost:8810 。（或终端里 `python3 工作台/app.py`）
开演前点右上角「重置」→ 回到还没跑过的空白状态。断网可用，不连任何广告后台。

讲之前可以先放一张图：页面右上角「流程图」（或直接打开 `流程图/工作台运转流程.html`，右上角有「演示」和「导出」）。它讲的是一圈 Loop 里人、Agent、状态库各干什么。

## 七步
| # | 你做什么 | 屏幕上应该出现 | 你说的一句话 |
|---|---|---|---|
| 1 | 点「跑第一圈」 | 顶部六个格子依次亮起：读取状态 → 判断下一步 → 执行 → 验证 → 写入状态 → 判断停止条件；几秒后整页长出来 | 这就是 Loop 的六个动作。它跑完会停下来等人 |
| 2 | 看 ① 发现异常 | 库存「严重」、退款率「要管」排在最前；泛词＋跑偏词是「正常」 | 没有目标，就没有异常。你以为的问题（跑偏词）不是问题 |
| 3 | 看 ② 判断原因，切「库存」「头部词花在哪」两个页签 | 事实／推断／未知三栏；主力规格快断货；头部词精确组只占一成 | 事实能复算，推断写明什么情况下不成立，未知写明去哪取 |
| 4 | 看 ③ 制定动作，点一下 D 再点掉 | 五条路各自的代价；D「只做否词」最多省几百美元、回测里一半误伤 | AI 摆选项和代价，不替你选。取舍是人的 |
| 5 | 到 ④：展开「暂停广告产品」，确认 1 条、驳回 1 条并**写理由**；把右上角身份切成「供应链」再点确认 | 状态条变色；驳回不写理由提交不了；供应链点确认被拦：「这一步，供应链没有权限」 | 人只在这里点头或摇头。权限写在系统里，不靠自觉 |
| 6 | 身份切回老板，回 ①：到仓周数 **4 改成 1** → 保存 → 「跑一圈」 | 六格再亮一遍：读回「确认 1、驳回 1」→ 输入变了 → v2。④ 里 「暂停」全没了，换成十几条「下调预算」；没人点过的旧动作作废；你确认过的那条被标「⚠ 请复核」 | 输出跟着输入变，才不是按钮只变颜色。模型可以忘，系统状态不能忘 |
| 7 | 点「原样再触发」→「跑一圈」；再把到仓周数清空 → 保存 →「跑一圈」 | 第一次：第 2 格写「同一份输入不重复跑」；第二次：第 2 格变红，任务标「遇到问题」，上一份结果不动 | 没有停止条件的自动化，不是员工，是事故 |

收尾：滑到 ⑤，指行动卡第 9 格和「操作记录」——谁、什么时候、做了什么，全在库里。

## 兜底
页面起不来：打开 `运行报告-R-001.html`（静态页，内容同一轮），照实说「这是开课前跑好的」。

## 照实说的三句
- 「跑一圈」是手动点的，定时没接；接上定时器它就是每周一自己跑。
- 「已确认」之后仍然是人去广告后台执行，AI 全程只读；「导出已确认清单」就是交给运营的那张表。
- 屏幕上是脱敏数据：代号、乘过系数的量和单价，比率是真的。

## 目录
`工作台/app.py` 后端（标准库＋SQLite）｜`工作台/index.html` 页面（离线可用）｜`_脚本/pipeline.py` 算数、规则、验证器｜`_脚本/render.py` 三栏表、行动卡、报告｜`_原始拉取/` 脱敏数据｜`参数.json` 输入与活动分工｜`工作台.db` 状态库（重置会删掉重建）
换成自己的数据跑：`python3 工作台/app.py --root <你的数据目录> --port 8811`（真实数据只留在自己电脑上）
